<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#FAFAFA"
    tools:context="com.example.intentandintentfilter.MainActivity">

    <!-- Button Call Phone -->
    <Button
        android:id="@+id/btncallphone"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Call Phone"
        android:textSize="24sp"
        android:textColor="@android:color/white"
        android:background="@drawable/btn_rounded_green"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Button Send SMS -->
    <Button
        android:id="@+id/btnsendsms"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="send sms"
        android:textSize="24sp"
        android:textColor="@android:color/white"
        android:background="@drawable/btn_rounded_blue"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/btncallphone"
        app:layout_constraintStart_toStartOf="@id/btncallphone"
        app:layout_constraintEnd_toEndOf="@id/btncallphone"/>

</androidx.constraintlayout.widget.ConstraintLayout>
